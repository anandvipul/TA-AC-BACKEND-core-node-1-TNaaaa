module.exports = {
    square: function (a) {
        return Math.pow(a,2);
    },
    rectangle: function (a,b) {
        return a*b;
    },
    circle: function (r) {
        return Math.PI*Math.pow(r,2);
    }
};